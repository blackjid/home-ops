---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: hass-postgres-secret
  namespace: home
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    name: hass-postgres-credentials
    creationPolicy: Owner
  dataFrom:
    - extract:
        # postgres-password, password
        key: hass-postgres
