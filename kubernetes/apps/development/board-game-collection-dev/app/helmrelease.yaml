---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: board-game-collection-dev
spec:
  chartRef:
    kind: OCIRepository
    name: board-game-collection-dev
  interval: 15m
  values:
    controllers:
      board-game-collection-dev:
        annotations:
          reloader.stakater.com/auto: "true"
        containers:
          app:
            image:
              repository: ghcr.io/blackjid/board-game-collection
              tag: latest@sha256:18182261b52f08c99f1a9770fc6c5cf83247e6e7e62f191b36b5f86cd1523178
            env:
              TZ: America/Santiago
              DATA_PATH: /data
              PORT: &port 3000
              NEXT_PUBLIC_BASE_URL: "https://ludo-test.blkh.dev"
            probes:
              liveness: &probes
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /
                    port: *port
                  initialDelaySeconds: 0
                  periodSeconds: 10
                  timeoutSeconds: 1
                  failureThreshold: 3
              readiness: *probes
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities:
                drop:
                  - ALL
            resources: {}
    defaultPodOptions:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
    service:
      app:
        controller: board-game-collection-dev
        ports:
          http:
            port: *port
    route:
      app:
        annotations:
          gatus.home-operations.com/endpoint: |-
            conditions: ["[STATUS] == 200"]
        hostnames:
          - "ludo-test.blkh.dev"
        parentRefs:
          - name: envoy-external
            namespace: network
        rules:
          - backendRefs:
              - identifier: app
                port: *port
    persistence:
      data:
        existingClaim: "{{ .Release.Name }}"
        globalMounts:
          - path: /data
      app-cache:
        type: emptyDir
        globalMounts:
          - path: /app/.next/cache
      tmp:
        type: emptyDir
      chromium-cache:
        type: emptyDir
        globalMounts:
          - path: /home/node/.cache
      chromium-config:
        type: emptyDir
        globalMounts:
          - path: /home/node/.config
